{
  "name": "visitor-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix visitor",
    "server": "npm run start --prefix server",
    "build": "npm run build --prefix visitor",
    "start": "npm run start --prefix visitor"
  }
}
