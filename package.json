{
  "name": "clients-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix clients",
    "server": "npm run start --prefix server",
    "build": "npm run build --prefix clients",
    "start": "npm run start --prefix clients"
  }
}
